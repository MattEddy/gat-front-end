Edit pages
<div class="edit-email">
  <div class="form-group">
    {{input type="text" value=name class="form-control"}}
  </div>

  <div class="form-group">
    {{textarea value=example_data class="form-control json-input"}}
  </div>

  <button class="btn btn-default" {{action 'save'}}>Save</button>
</div>
